/*
** EPITECH PROJECT, 2020
** myteams
** File description:
** execution of the server's commands
*/
        //Echo back the message that came in  
        // else {
            // logout(data, i);
            //set the string terminating NULL byte on the end  
            //of the data read  
        //     data.buffer[readvalue] = '\0';
        //     send(data.fd, data.buffer, strlen(data.buffer), 0);
        // }
